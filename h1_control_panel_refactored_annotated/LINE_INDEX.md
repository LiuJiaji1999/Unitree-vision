# 逐行注释索引\n\n源码中已包含中文行内注释。此索引用于快速定位非空代码行。\n\n| 行号 | 代码 |\n|---:|---|\n| 1 | `#!/usr/bin/env python3` |\n| 2 | `# -*- coding: utf-8 -*-` |\n| 4 | `"""` |\n| 5 | `H1 机器人控制面板 - 工程化精简注释版。` |\n| 7 | `本版本做了三件事：` |\n| 8 | `1. 删除未启用的参数页、视频流占位、大段旧注释和未使用导入。` |\n| 9 | `2. 按“配置 / 工具函数 / 数据解析 / 后台线程 / UI / 程序入口”组织代码。` |\n| 10 | `3. 对每个可执行代码块添加中文注释，便于继续拆分成多文件工程。` |\n| 12 | `运行：` |\n| 13 | `pip install PyQt5 matplotlib numpy` |\n| 14 | `python3 h1_control_panel_engineered_annotated.py` |\n| 15 | `"""` |\n| 17 | `from __future__ import annotations  # 允许类型注解引用尚未定义的类。` |\n| 19 | `import hashlib  # 用于演示账号密码哈希。` |\n| 20 | `import json  # 用于读写 users.json 和 h1_config.json。` |\n| 21 | `import math  # 用于角度转换和点云抽样计算。` |\n| 22 | `import random  # 用于 mock 模式生成模拟数据。` |\n| 23 | `import re  # 用于清洗终端输出中的 ANSI 控制码。` |\n| 24 | `import shlex  # 用于安全拼接 SSH 命令。` |\n| 25 | `import sys  # 用于 Qt argv 和程序退出码。` |\n| 26 | `import time  # 用于线程时间、超时判断和 mock 时间。` |\n| 27 | `from dataclasses import asdict, dataclass  # 用于配置对象和 JSON 保存。` |\n| 28 | `from datetime import datetime  # 用于日志时间戳。` |\n| 29 | `from pathlib import Path  # 用于跨平台路径处理。` |\n| 30 | `from typing import Any, Dict, List, Optional, Sequence, Tuple  # 用于类型提示。` |\n| 32 | `import numpy as np  # 用于读取和处理 PCD 点云。` |\n| 33 | `from matplotlib import font_manager, rcParams  # 用于设置 Matplotlib 中文字体。` |\n| 34 | `from matplotlib.backends.backend_qt5agg import FigureCanvasQTAgg as FigureCanvas  # Qt 内嵌 Matplotlib 画布。` |\n| 35 | `from matplotlib.figure import Figure  # Matplotlib 图对象。` |\n| 36 | `from matplotlib.ticker import MaxNLocator  # 用于控制坐标轴刻度数量。` |\n| 37 | `from PyQt5.QtCore import QObject, QProcess, QThread, QTimer, Qt, pyqtSignal  # Qt 核心类。` |\n| 38 | `from PyQt5.QtGui import QFont  # Qt 字体类。` |\n| 39 | `from PyQt5.QtWidgets import (  # Qt 常用控件。` |\n| 40 | `QApplication, QCheckBox, QComboBox, QDialog, QFileDialog, QFormLayout,` |\n| 41 | `QGridLayout, QGroupBox, QHBoxLayout, QHeaderView, QLabel, QLineEdit,` |\n| 42 | `QMainWindow, QMessageBox, QPushButton, QScrollArea, QSizePolicy, QSpinBox,` |\n| 43 | `QSplitter, QStatusBar, QTabWidget, QTableWidget, QTableWidgetItem,` |\n| 44 | `QTextEdit, QVBoxLayout, QWidget,` |\n| 45 | `)` |\n| 47 | `APP_DIR = Path(__file__).resolve().parent  # 程序所在目录。` |\n| 48 | `USERS_FILE = APP_DIR / "users.json"  # 用户账号文件。` |\n| 49 | `CONFIG_FILE = APP_DIR / "h1_config.json"  # 连接配置文件。` |\n| 50 | `ANSI_RE = re.compile(r"\x1B(?:[@-Z\\-_]\|\[[0-?]*[ -/]*[@-~])")  # 标准 ANSI 控制码。` |\n| 51 | `BROKEN_COLOR_RE = re.compile(r"(?<!\w)(?:\[[0-9;]*m\|[0-9;]*m)")  # 残缺颜色码。` |\n| 52 | `KEY_RE = re.compile(r"Key\s+pressed\.", re.IGNORECASE)  # demo 输出中无用的按键提示。` |\n| 54 | `UI_TEXT = {  # 字段到中文文案的映射。` |\n| 55 | `"update_time": "更新时间", "packet_count": "数据包计数", "topic": "主题",` |\n| 56 | `"idl_type": "数据类型", "index": "编号", "head": "帧头",` |\n| 57 | `"foot_force": "足端力", "tick": "计时器", "wireless_remote": "遥控器原始数据",` |\n| 58 | `"bit_flag": "组件状态", "adc_reel": "卷线器电流", "temperature_ntc1": "主板中心温度",` |\n| 59 | `"temperature_ntc2": "自动充电温度", "power_v": "电池电压", "power_a": "电池电流",` |\n| 60 | `"fan_frequency": "风扇转速", "crc": "校验位", "imu_state": "惯性测量单元状态",` |\n| 61 | `"bms_state": "电池管理系统状态", "bms_status_display": "电池状态",` |\n| 62 | `"bms_soc_display": "电池电量", "motor_state_count": "电机数量",` |\n| 63 | `"battery_voltage_display": "电池电压显示", "battery_current_display": "电池电流显示",` |\n| 64 | `"quaternion": "四元数", "rpy": "姿态角", "rpy_deg": "姿态角（度）",` |\n| 65 | `"gyroscope": "陀螺仪", "accelerometer": "加速度计", "temperature": "温度",` |\n| 66 | `"mode": "模式", "q": "关节位置", "dq": "关节速度", "ddq": "关节加速度",` |\n| 67 | `"tau_est": "估算力矩", "lost": "通信丢失", "error_flag": "错误标志",` |\n| 68 | `"comm_frequency": "通信频率", "stamp": "时间戳", "firmware_version": "固件版本",` |\n| 69 | `"software_version": "软件版本", "sdk_version": "SDK 版本", "sys_rotation_speed": "系统转速",` |\n| 70 | `"com_rotation_speed": "通信转速", "error_state": "错误状态",` |\n| 71 | `"error_state_text": "错误状态说明", "cloud_frequency": "点云频率",` |\n| 72 | `"cloud_packet_loss_rate": "点云丢包率", "cloud_size": "点云数量",` |\n| 73 | `"cloud_scan_num": "点云扫描帧数", "imu_frequency": "惯导频率",` |\n| 74 | `"imu_packet_loss_rate": "惯导丢包率", "imu_rpy": "惯导姿态角",` |\n| 75 | `"imu_rpy_deg": "惯导姿态角（度）", "serial_recv_stamp": "串口接收时间戳",` |\n| 76 | `"serial_buffer_size": "串口缓存大小", "serial_buffer_read": "串口已读大小",` |\n| 77 | `}` |\n| 79 | `BMS_STATUS = {  # BMS 状态码说明。` |\n| 80 | `0: "SAFE（未开启电池）", 1: "WAKE_UP（唤醒事件）", 6: "PRECHG（预充电）",` |\n| 81 | `7: "CHG（充电）", 8: "DCHG（放电）", 9: "SELF_DCHG（自放电）",` |\n| 82 | `11: "ALARM（警告）", 12: "RESET_ALARM（等待复位）", 13: "AUTO_RECOVERY（自动恢复）",` |\n| 83 | `}` |\n| 85 | `_DDS_INITIALIZED = False  # DDS 是否已初始化。` |\n| 86 | `_DDS_IFACE = ""  # DDS 首次绑定的网卡。` |\n| 89 | `@dataclass` |\n| 90 | `class RobotConfig:` |\n| 91 | `"""机器人运行配置。"""` |\n| 92 | `robot_ip: str = "192.168.123.162"  # 机器人 IP。` |\n| 93 | `port: int = 8080  # 预留端口。` |\n| 94 | `network_interface: str = "enx9c69d3565ef9"  # 本机 DDS 网卡。` |\n| 95 | `protocol: str = "sdk2"  # mock 或 sdk2。` |\n| 96 | `timeout_ms: int = 2000  # 预留超时。` |\n| 97 | `lowstate_topic: str = "rt/lowstate"  # LowState topic。` |\n| 98 | `lowstate_idl: str = "unitree_go"  # LowState IDL。` |\n| 99 | `lidar_state_topic: str = "rt/utlidar/lidar_state"  # LiDAR topic。` |\n| 100 | `pointcloud_file: str = str(APP_DIR / "GlobalMap.pcd")  # 默认 PCD 文件。` |\n| 103 | `def now_text() -> str:` |\n| 104 | `"""返回日志时间。"""` |\n| 105 | `return datetime.now().strftime("%Y-%m-%d %H:%M:%S")  # 统一时间格式。` |\n| 108 | `def tr(text: Any) -> str:` |\n| 109 | `"""字段名翻译。"""` |\n| 110 | `return UI_TEXT.get(str(text), str(text))  # 映射不到则原样返回。` |\n| 113 | `def setup_matplotlib_font() -> None:` |\n| 114 | `"""设置 Matplotlib 中文字体。"""` |\n| 115 | `for p in ["/usr/share/fonts/opentype/noto/NotoSansCJK-Regular.ttc", "/usr/share/fonts/truetype/wqy/wqy-microhei.ttc"]:  # 常见字体路径。` |\n| 116 | `path = Path(p)  # 转成 Path。` |\n| 117 | `if path.exists():  # 如果字体存在。` |\n| 118 | `font_manager.fontManager.addfont(str(path))  # 注册字体。` |\n| 119 | `name = font_manager.FontProperties(fname=str(path)).get_name()  # 获取字体名。` |\n| 120 | `rcParams["font.sans-serif"] = [name, "DejaVu Sans"]  # 设置优先字体。` |\n| 121 | `rcParams["axes.unicode_minus"] = False  # 修复负号显示。` |\n| 122 | `return  # 设置成功后返回。` |\n| 123 | `rcParams["font.sans-serif"] = ["Noto Sans CJK SC", "Microsoft YaHei", "SimHei", "DejaVu Sans"]  # 兜底字体名。` |\n| 124 | `rcParams["axes.unicode_minus"] = False  # 修复负号显示。` |\n| 127 | `def clean_terminal(text: str) -> str:` |\n| 128 | `"""清洗远程终端输出。"""` |\n| 129 | `text = str(text).replace("\r\n", "\n").replace("\r", "\n")  # 统一换行。` |\n| 130 | `text = ANSI_RE.sub("", text)  # 删除 ANSI 控制码。` |\n| 131 | `text = BROKEN_COLOR_RE.sub("", text)  # 删除残缺颜色码。` |\n| 132 | `lines = []  # 保存有效行。` |\n| 133 | `for line in text.splitlines():  # 按行处理。` |\n| 134 | `if not line.strip():  # 跳过空行。` |\n| 135 | `continue` |\n| 136 | `if KEY_RE.fullmatch(line.strip()):  # 跳过 Key pressed 行。` |\n| 137 | `continue` |\n| 138 | `lines.append(line)  # 保存有效行。` |\n| 139 | `return "\n".join(lines)  # 拼回文本。` |\n| 142 | `def safe_get(obj: Any, name: str, default: Any = None) -> Any:` |\n| 143 | `"""安全读取 SDK 对象字段。"""` |\n| 144 | `try:  # getattr 可能因为 SDK 生成类异常而失败。` |\n| 145 | `return getattr(obj, name, default)  # 读取字段。` |\n| 146 | `except Exception:  # 读取失败。` |\n| 147 | `return default  # 返回默认值。` |\n| 150 | `def to_list(value: Any) -> List[Any]:` |\n| 151 | `"""把 SDK 数组转换为 list。"""` |\n| 152 | `if value is None:  # None 转空列表。` |\n| 153 | `return []` |\n| 154 | `try:  # 尝试迭代。` |\n| 155 | `return list(value)  # 返回列表。` |\n| 156 | `except Exception:  # 不可迭代。` |\n| 157 | `return [value]  # 单值包装。` |\n| 160 | `def as_int(value: Any) -> Optional[int]:` |\n| 161 | `"""安全转 int。"""` |\n| 162 | `try:  # 尝试转换。` |\n| 163 | `return int(value) if value is not None else None  # None 保持 None。` |\n| 164 | `except Exception:  # 转换失败。` |\n| 165 | `return None  # 返回 None。` |\n| 168 | `def as_float(value: Any) -> Optional[float]:` |\n| 169 | `"""安全转 float。"""` |\n| 170 | `try:  # 尝试转换。` |\n| 171 | `return float(value) if value is not None else None  # None 保持 None。` |\n| 172 | `except Exception:  # 转换失败。` |\n| 173 | `return None  # 返回 None。` |\n| 176 | `def fmt(value: Any, digits: int = 3) -> str:` |\n| 177 | `"""格式化浮点文本。"""` |\n| 178 | `n = as_float(value)  # 转成浮点。` |\n| 179 | `return "N/A" if n is None else f"{n:.{digits}f}"  # 无效值显示 N/A。` |\n| 182 | `def display(value: Any, max_len: int = 1200) -> str:` |\n| 183 | `"""把任意字段转换为表格文本。"""` |\n| 184 | `if value is None:  # None 统一显示。` |\n| 185 | `return "N/A"` |\n| 186 | `if isinstance(value, float):  # 浮点保留 6 位。` |\n| 187 | `return f"{value:.6f}"` |\n| 188 | `if isinstance(value, (int, bool, str)):  # 基础类型直接显示。` |\n| 189 | `return str(value)` |\n| 190 | `try:  # 尝试处理数组。` |\n| 191 | `text = "[" + ", ".join(display(v, 80) for v in list(value)) + "]"  # 递归格式化数组。` |\n| 192 | `except Exception:  # 不是数组时。` |\n| 193 | `text = object_summary(value)  # 使用对象摘要。` |\n| 194 | `return text[:max_len] + " ..." if len(text) > max_len else text  # 限制长度。` |\n| 197 | `def vector_text(value: Any, digits: int = 3) -> str:` |\n| 198 | `"""格式化向量。"""` |\n| 199 | `arr = to_list(value)  # 转列表。` |\n| 200 | `return "N/A" if not arr else "[" + ", ".join(fmt(v, digits) if as_float(v) is not None else str(v) for v in arr) + "]"  # 生成向量文本。` |\n| 203 | `def rpy_deg_text(value: Any) -> str:` |\n| 204 | `"""RPY 弧度转角度。"""` |\n| 205 | `rpy = to_list(value)  # 转列表。` |\n| 206 | `if len(rpy) < 3:  # 不足三轴。` |\n| 207 | `return "N/A"` |\n| 208 | `nums = [as_float(v) for v in rpy[:3]]  # 转浮点。` |\n| 209 | `if any(v is None for v in nums):  # 有无效值。` |\n| 210 | `return "N/A"` |\n| 211 | `return vector_text([math.degrees(v) for v in nums], 2)  # 返回角度文本。` |\n| 214 | `def temp_text(value: Any) -> str:` |\n| 215 | `"""显示 int8/uint8 温度。"""` |\n| 216 | `t = as_int(value)  # 转整数。` |\n| 217 | `if t is None:  # 无效值。` |\n| 218 | `return "N/A"` |\n| 219 | `if t > 127:  # uint8 转 int8。` |\n| 220 | `t -= 256` |\n| 221 | `return f"{t} ℃"  # 返回摄氏度。` |\n| 224 | `def field_names(obj: Any, preferred: Sequence[str] = ()) -> List[str]:` |\n| 225 | `"""获取对象字段名。"""` |\n| 226 | `if obj is None:  # 无对象。` |\n| 227 | `return []` |\n| 228 | `ann = getattr(obj.__class__, "__annotations__", {})  # 优先用注解。` |\n| 229 | `names = list(ann.keys()) if ann else [n for n in dir(obj) if not n.startswith("_") and not callable(safe_get(obj, n, None))]  # 取字段。` |\n| 230 | `ordered = [n for n in preferred if n in names]  # 常用字段优先。` |\n| 231 | `ordered += [n for n in names if n not in ordered]  # 补齐其他字段。` |\n| 232 | `return ordered  # 返回字段名。` |\n| 235 | `def object_summary(obj: Any) -> str:` |\n| 236 | `"""生成 SDK 对象摘要。"""` |\n| 237 | `if obj is None:  # 空对象。` |\n| 238 | `return "N/A"` |\n| 239 | `if isinstance(obj, (int, float, bool, str)):  # 基础类型。` |\n| 240 | `return display(obj)` |\n| 241 | `parts = []  # 保存字段摘要。` |\n| 242 | `for name in field_names(obj)[:12]:  # 最多展示 12 个字段。` |\n| 243 | `value = safe_get(obj, name, None)  # 读取字段。` |\n| 244 | `parts.append(f"{name}={display(value, 60)}")  # 添加摘要。` |\n| 245 | `return "{" + ", ".join(parts) + ("..." if len(parts) >= 12 else "") + "}"  # 返回摘要。` |\n| 248 | `def object_table(obj: Any, preferred: Sequence[str] = ()) -> Dict[str, str]:` |\n| 249 | `"""对象转键值表。"""` |\n| 250 | `data: Dict[str, str] = {}  # 初始化字典。` |\n| 251 | `for name in field_names(obj, preferred):  # 遍历字段。` |\n| 252 | `data[name] = display(safe_get(obj, name, None))  # 保存字段显示值。` |\n| 253 | `return data  # 返回表格数据。` |\n| 256 | `def bms_status_text(value: Any) -> str:` |\n| 257 | `"""BMS 状态码转文字。"""` |\n| 258 | `code = as_int(value)  # 转整数。` |\n| 259 | `return "N/A" if code is None else f"{code} {BMS_STATUS.get(code, '未知状态')}"  # 返回状态文本。` |\n| 262 | `def cell_summary(cell_vol: Any) -> str:` |\n| 263 | `"""电芯电压概览。"""` |\n| 264 | `vals = []  # 保存有效电芯。` |\n| 265 | `for i, raw in enumerate(to_list(cell_vol)[:15], 1):  # 遍历最多 15 节。` |\n| 266 | `mv = as_int(raw)  # 原始 mV。` |\n| 267 | `if mv and mv > 0:  # 有效电压。` |\n| 268 | `vals.append((i, mv / 1000.0))  # 转为 V。` |\n| 269 | `if not vals:  # 无有效数据。` |\n| 270 | `return "N/A"` |\n| 271 | `total = sum(v for _, v in vals)  # 总电压。` |\n| 272 | `mn = min(vals, key=lambda x: x[1])  # 最低单体。` |\n| 273 | `mx = max(vals, key=lambda x: x[1])  # 最高单体。` |\n| 274 | `return f"有效单体数={len(vals)}，估算总电压={total:.2f} V，最低=第{mn[0]:02d}节 {mn[1]:.3f} V，最高=第{mx[0]:02d}节 {mx[1]:.3f} V，压差={(mx[1]-mn[1])*1000:.0f} ...` |\n| 277 | `def extract_motor_rows(motor_state: Any) -> Dict[str, Any]:` |\n| 278 | `"""提取电机表格。"""` |\n| 279 | `columns = ["index", "mode", "q", "dq", "ddq", "tau_est", "temperature", "lost", "error_flag", "comm_frequency"]  # 表头。` |\n| 280 | `rows = []  # 行数据。` |\n| 281 | `for index, motor in enumerate(to_list(motor_state)):  # 遍历电机。` |\n| 282 | `reserve = to_list(safe_get(motor, "reserve", []))  # reserve 字段。` |\n| 283 | `rows.append({  # 添加一行。` |\n| 284 | `"index": str(index), "mode": display(safe_get(motor, "mode", None)),` |\n| 285 | `"q": display(safe_get(motor, "q", None)), "dq": display(safe_get(motor, "dq", None)),` |\n| 286 | `"ddq": display(safe_get(motor, "ddq", None)), "tau_est": display(safe_get(motor, "tau_est", None)),` |\n| 287 | `"temperature": display(safe_get(motor, "temperature", None)), "lost": display(safe_get(motor, "lost", None)),` |\n| 288 | `"error_flag": display(reserve[0] if len(reserve) > 0 else None),` |\n| 289 | `"comm_frequency": display(reserve[1] if len(reserve) > 1 else None),` |\n| 290 | `})` |\n| 291 | `return {"columns": columns, "rows": rows}  # 返回表头和行。` |\n| 294 | `def extract_low_state(msg: Any, cfg: RobotConfig, count: int) -> Dict[str, Any]:` |\n| 295 | `"""LowState 消息转 UI 数据。"""` |\n| 296 | `imu = safe_get(msg, "imu_state", None)  # IMU 子结构。` |\n| 297 | `bms = safe_get(msg, "bms_state", None)  # BMS 子结构。` |\n| 298 | `motors = safe_get(msg, "motor_state", [])  # 电机数组。` |\n| 299 | `main = {"update_time": now_text(), "packet_count": str(count), "topic": cfg.lowstate_topic, "idl_type": cfg.lowstate_idl}  # 主信息。` |\n| 300 | `for name in ["head", "foot_force", "tick", "wireless_remote", "bit_flag", "adc_reel", "temperature_ntc1", "temperature_ntc2", "power_v", ...` |\n| 301 | `value = safe_get(msg, name, None)  # 读取字段。` |\n| 302 | `main[name] = temp_text(value) if name.startswith("temperature_ntc") else display(value)  # 温度特殊处理。` |\n| 303 | `main["imu_state"] = object_summary(imu)  # IMU 摘要。` |\n| 304 | `main["bms_state"] = object_summary(bms)  # BMS 摘要。` |\n| 305 | `main["bms_status_display"] = bms_status_text(safe_get(bms, "status", None))  # BMS 状态。` |\n| 306 | `soc = as_int(safe_get(bms, "soc", None))  # SOC。` |\n| 307 | `main["bms_soc_display"] = f"{soc} %" if soc is not None else "N/A"  # SOC 显示。` |\n| 308 | `main["bms_cell_voltage_summary"] = cell_summary(safe_get(bms, "cell_vol", None))  # 电芯摘要。` |\n| 309 | `main["motor_state_count"] = str(len(to_list(motors)))  # 电机数量。` |\n| 310 | `pv = as_float(safe_get(msg, "power_v", None))  # 总电压。` |\n| 311 | `pa = as_float(safe_get(msg, "power_a", None))  # 总电流。` |\n| 312 | `main["battery_voltage_display"] = f"{pv:.2f} V" if pv is not None else "N/A"  # 电压显示。` |\n| 313 | `main["battery_current_display"] = f"{pa:.2f} A" if pa is not None else "N/A"  # 电流显示。` |\n| 314 | `imu_table = object_table(imu, ["quaternion", "rpy", "gyroscope", "accelerometer", "temperature"])  # IMU 表。` |\n| 315 | `imu_table["rpy_deg"] = rpy_deg_text(safe_get(imu, "rpy", None))  # 角度制 RPY。` |\n| 316 | `bms_table = {  # BMS 表。` |\n| 317 | `"LowState 总电压 power_v": main["battery_voltage_display"],` |\n| 318 | `"LowState 总电流 power_a": main["battery_current_display"],` |\n| 319 | `"BMS 状态": main["bms_status_display"],` |\n| 320 | `"SOC 电量": main["bms_soc_display"],` |\n| 321 | `"15 节电芯电压概览": main["bms_cell_voltage_summary"],` |\n| 322 | `}` |\n| 323 | `motor_data = extract_motor_rows(motors)  # 电机表。` |\n| 324 | `return {"lowstate_main": main, "imu_state": imu_table, "bms_state": bms_table, "motor_columns": motor_data["columns"], "motor_rows": moto...` |\n| 327 | `def lidar_error_text(value: Any) -> str:` |\n| 328 | `"""LiDAR 错误码解释。"""` |\n| 329 | `code = as_int(value)  # 转整数。` |\n| 330 | `if code is None:  # 无效值。` |\n| 331 | `return "N/A"` |\n| 332 | `if code == 0:  # 正常。` |\n| 333 | `return "0 正常"` |\n| 334 | `mapping = [(0x01, "电机转速异常"), (0x02, "点云数据异常"), (0x04, "串口数据异常")]  # 错误位。` |\n| 335 | `names = [text for bit, text in mapping if code & bit]  # 解析位图。` |\n| 336 | `return f"{code} / " + "，".join(names) if names else f"{code} 未知错误码"  # 返回说明。` |\n| 339 | `def extract_lidar_state(msg: Any, cfg: RobotConfig, count: int) -> Dict[str, str]:` |\n| 340 | `"""LiDAR State 消息转 UI 数据。"""` |\n| 341 | `data = {"update_time": now_text(), "packet_count": str(count), "topic": cfg.lidar_state_topic, "idl_type": "unitree_go/LidarState_"}  # 基...` |\n| 342 | `fields = ["stamp", "firmware_version", "software_version", "sdk_version", "sys_rotation_speed", "com_rotation_speed", "error_state", "clo...` |\n| 343 | `for name in fields:  # 遍历字段。` |\n| 344 | `data[name] = display(safe_get(msg, name, None))  # 填充值。` |\n| 345 | `data["imu_rpy_deg"] = rpy_deg_text(safe_get(msg, "imu_rpy", None))  # RPY 角度。` |\n| 346 | `data["error_state_text"] = lidar_error_text(safe_get(msg, "error_state", None))  # 错误说明。` |\n| 347 | `return data  # 返回表格数据。` |\n| 350 | `def import_lowstate_class(idl_type: str) -> Any:` |\n| 351 | `"""延迟导入 LowState IDL。"""` |\n| 352 | `if idl_type == "unitree_go":  # go IDL。` |\n| 353 | `from unitree_sdk2py.idl.unitree_go.msg.dds_ import LowState_  # 真机环境才需要。` |\n| 354 | `return LowState_  # 返回类。` |\n| 355 | `if idl_type == "unitree_hg":  # hg IDL。` |\n| 356 | `from unitree_sdk2py.idl.unitree_hg.msg.dds_ import LowState_  # 真机环境才需要。` |\n| 357 | `return LowState_  # 返回类。` |\n| 358 | `raise RuntimeError(f"未知 LowState IDL：{idl_type}")  # 配置错误。` |\n| 361 | `def ensure_dds_initialized(iface: str, log=None) -> None:` |\n| 362 | `"""DDS 只初始化一次。"""` |\n| 363 | `global _DDS_INITIALIZED, _DDS_IFACE  # 修改全局状态。` |\n| 364 | `from unitree_sdk2py.core.channel import ChannelFactoryInitialize  # 延迟导入 SDK。` |\n| 365 | `iface = (iface or "").strip()  # 清理网卡名。` |\n| 366 | `if _DDS_INITIALIZED:  # 已初始化。` |\n| 367 | `if log and iface and iface != _DDS_IFACE:  # 新网卡不同。` |\n| 368 | `log(f"DDS 已初始化过，继续使用首次网卡：{_DDS_IFACE}，忽略新的网卡：{iface}")  # 提示。` |\n| 369 | `return  # 不重复初始化。` |\n| 370 | `if iface:  # 指定网卡。` |\n| 371 | `if log: log(f"初始化 Unitree DDS，绑定网卡：{iface}")  # 日志。` |\n| 372 | `ChannelFactoryInitialize(0, iface)  # 初始化 DDS。` |\n| 373 | `_DDS_IFACE = iface  # 记录网卡。` |\n| 374 | `else:  # 未指定网卡。` |\n| 375 | `if log: log("初始化 Unitree DDS：使用 SDK 默认接口。")  # 日志。` |\n| 376 | `ChannelFactoryInitialize(0)  # 默认初始化。` |\n| 377 | `_DDS_INITIALIZED = True  # 标记完成。` |\n| 380 | `class UserStore:` |\n| 381 | `"""演示用户认证。"""` |\n| 382 | `def __init__(self, path: Path = USERS_FILE) -> None:  # 初始化。` |\n| 383 | `self.path = path  # 保存路径。` |\n| 384 | `self.ensure_defaults()  # 确保默认账号存在。` |\n| 386 | `@staticmethod` |\n| 387 | `def password_hash(password: str) -> str:  # 密码哈希。` |\n| 388 | `return hashlib.sha256(("h1-demo-salt:" + password).encode("utf-8")).hexdigest()  # 加盐 SHA256。` |\n| 390 | `def ensure_defaults(self) -> None:  # 创建默认账号。` |\n| 391 | `if self.path.exists():  # 文件存在则不覆盖。` |\n| 392 | `return` |\n| 393 | `users = {  # 默认账号。` |\n| 394 | `"admin": {"password_hash": self.password_hash("admin123"), "role": "admin", "display_name": "管理员"},` |\n| 395 | `"operator": {"password_hash": self.password_hash("operator123"), "role": "operator", "display_name": "操作员"},` |\n| 396 | `}` |\n| 397 | `self.path.write_text(json.dumps(users, ensure_ascii=False, indent=2), encoding="utf-8")  # 写入文件。` |\n| 399 | `def authenticate(self, username: str, password: str) -> Optional[Dict[str, str]]:  # 登录校验。` |\n| 400 | `users = json.loads(self.path.read_text(encoding="utf-8"))  # 读取用户表。` |\n| 401 | `user = users.get(username)  # 查找用户。` |\n| 402 | `if not user or user.get("password_hash") != self.password_hash(password):  # 不存在或密码错误。` |\n| 403 | `return None` |\n| 404 | `return {"username": username, "role": user.get("role", "operator"), "display_name": user.get("display_name", username)}  # 返回资料。` |\n| 407 | `class LowStateWorker(QThread):` |\n| 408 | `"""LowState 后台线程。"""` |\n| 409 | `status_signal = pyqtSignal(dict)  # 状态信号。` |\n| 410 | `log_signal = pyqtSignal(str)  # 日志信号。` |\n| 411 | `fatal_signal = pyqtSignal(str)  # 致命错误信号。` |\n| 412 | `ready_signal = pyqtSignal()  # 初始化成功信号。` |\n| 414 | `def __init__(self, cfg: RobotConfig, parent=None) -> None:  # 初始化。` |\n| 415 | `super().__init__(parent)  # 初始化线程。` |\n| 416 | `self.cfg = cfg  # 保存配置。` |\n| 417 | `self.running = True  # 运行标志。` |\n| 418 | `self.subscriber = None  # SDK 订阅器。` |\n| 419 | `self.count = 0  # 包计数。` |\n| 420 | `self.last_msg = 0.0  # 最近消息时间。` |\n| 421 | `self.last_emit = 0.0  # 最近 UI 刷新时间。` |\n| 423 | `def stop(self) -> None:  # 停止线程。` |\n| 424 | `self.running = False  # 请求停止。` |\n| 425 | `if self.subscriber:  # 如果订阅器存在。` |\n| 426 | `for name in ["Close", "close", "Stop", "stop", "Destroy", "destroy"]:  # 兼容关闭函数。` |\n| 427 | `fn = getattr(self.subscriber, name, None)  # 获取函数。` |\n| 428 | `if callable(fn):  # 可调用。` |\n| 429 | `try: fn(); break  # 尝试关闭。` |\n| 430 | `except Exception: pass  # 忽略关闭异常。` |\n| 432 | `def run(self) -> None:  # 线程入口。` |\n| 433 | `if self.cfg.protocol == "mock":  # mock 模式。` |\n| 434 | `self.run_mock(); return` |\n| 435 | `if self.cfg.protocol == "sdk2":  # SDK2 模式。` |\n| 436 | `self.run_sdk2(); return` |\n| 437 | `self.fatal_signal.emit(f"只支持 mock/sdk2，当前 protocol={self.cfg.protocol}")  # 不支持协议。` |\n| 439 | `def run_mock(self) -> None:  # mock 数据。` |\n| 440 | `self.log_signal.emit("mock LowState 线程启动。")  # 日志。` |\n| 441 | `self.ready_signal.emit()  # mock 立即就绪。` |\n| 442 | `start = time.monotonic()  # 起始时间。` |\n| 443 | `while self.running:  # 循环生成数据。` |\n| 444 | `self.count += 1  # 包计数。` |\n| 445 | `t = time.monotonic() - start  # 运行时间。` |\n| 446 | `rpy = [0.02 * math.sin(t), 0.04 * math.sin(t * 0.6), 0.12 * math.sin(t * 0.2)]  # 模拟姿态。` |\n| 447 | `status = self.mock_status(rpy)  # 构造状态。` |\n| 448 | `self.status_signal.emit(status)  # 发给 UI。` |\n| 449 | `self.msleep(100)  # 10Hz。` |\n| 450 | `self.log_signal.emit("mock LowState 线程已停止。")  # 日志。` |\n| 452 | `def mock_status(self, rpy: List[float]) -> Dict[str, Any]:  # 构造 mock 状态。` |\n| 453 | `motors = []  # 电机行。` |\n| 454 | `for i in range(20):  # 20 个电机。` |\n| 455 | `motors.append({"index": str(i), "mode": "0", "q": fmt(random.uniform(-0.1, 0.1), 6), "dq": fmt(random.uniform(-0.05, 0.05), 6), "ddq": "0...` |\n| 456 | `return {  # 返回完整状态。` |\n| 457 | `"lowstate_main": {"update_time": now_text(), "packet_count": str(self.count), "topic": "mock/lowstate", "idl_type": "mock", "power_v": "6...` |\n| 458 | `"imu_state": {"quaternion": "[1, 0, 0, 0]", "rpy": vector_text(rpy, 6), "rpy_deg": rpy_deg_text(rpy), "gyroscope": vector_text([random.un...` |\n| 459 | `"bms_state": {"LowState 总电压 power_v": "67.20 V", "LowState 总电流 power_a": "1.50 A", "BMS 状态": "0 SAFE（未开启电池）", "SOC 电量": "80 %", "15 节电芯电压...` |\n| 460 | `"motor_columns": ["index", "mode", "q", "dq", "ddq", "tau_est", "temperature", "lost", "error_flag", "comm_frequency"],` |\n| 461 | `"motor_rows": motors,` |\n| 462 | `}` |\n| 464 | `def run_sdk2(self) -> None:  # SDK2 读取。` |\n| 465 | `try:  # 捕获 SDK 初始化错误。` |\n| 466 | `from unitree_sdk2py.core.channel import ChannelSubscriber  # 延迟导入订阅器。` |\n| 467 | `LowState = import_lowstate_class(self.cfg.lowstate_idl)  # 导入 IDL。` |\n| 468 | `ensure_dds_initialized(self.cfg.network_interface, self.log_signal.emit)  # 初始化 DDS。` |\n| 469 | `self.subscriber = ChannelSubscriber(self.cfg.lowstate_topic, LowState)  # 创建订阅器。` |\n| 470 | `self.subscriber.Init(self.on_message, 10)  # 初始化订阅。` |\n| 471 | `self.last_msg = time.monotonic()  # 初始化时间。` |\n| 472 | `self.ready_signal.emit()  # 通知就绪。` |\n| 473 | `warned = False  # 无数据提示标志。` |\n| 474 | `while self.running:  # 保持线程存活。` |\n| 475 | `if time.monotonic() - self.last_msg > 3 and not warned:  # 3 秒无数据。` |\n| 476 | `warned = True  # 避免重复提示。` |\n| 477 | `self.log_signal.emit("超过 3 秒未收到 LowState，请检查网卡、Topic、IDL、防火墙和 DDS。")  # 提示。` |\n| 478 | `self.msleep(100)  # 降低 CPU。` |\n| 479 | `except ModuleNotFoundError as exc:  # SDK 未安装。` |\n| 480 | `self.fatal_signal.emit(f"未找到 unitree_sdk2py，请先安装 Unitree SDK2 Python。原始错误：{exc}")  # 报错。` |\n| 481 | `except Exception as exc:  # 其他错误。` |\n| 482 | `self.fatal_signal.emit(f"SDK2 LowState 初始化失败：{exc}")  # 报错。` |\n| 484 | `def on_message(self, msg: Any) -> None:  # SDK 回调。` |\n| 485 | `if not self.running:  # 停止后忽略。` |\n| 486 | `return` |\n| 487 | `self.count += 1  # 包计数。` |\n| 488 | `self.last_msg = time.monotonic()  # 更新接收时间。` |\n| 489 | `if self.last_msg - self.last_emit < 0.1:  # UI 限速。` |\n| 490 | `return` |\n| 491 | `self.last_emit = self.last_msg  # 更新时间。` |\n| 492 | `try:  # 解析消息。` |\n| 493 | `self.status_signal.emit(extract_low_state(msg, self.cfg, self.count))  # 发状态。` |\n| 494 | `except Exception as exc:  # 解析失败。` |\n| 495 | `self.log_signal.emit(f"解析 LowState 失败：{exc}")  # 记日志。` |\n| 498 | `class LidarStateWorker(QThread):` |\n| 499 | `"""LiDAR 后台线程。"""` |\n| 500 | `status_signal = pyqtSignal(dict)  # 状态信号。` |\n| 501 | `log_signal = pyqtSignal(str)  # 日志信号。` |\n| 502 | `error_signal = pyqtSignal(str)  # 错误信号。` |\n| 504 | `def __init__(self, cfg: RobotConfig, parent=None) -> None:  # 初始化。` |\n| 505 | `super().__init__(parent)  # 初始化线程。` |\n| 506 | `self.cfg = cfg  # 保存配置。` |\n| 507 | `self.running = True  # 运行标志。` |\n| 508 | `self.subscriber = None  # 订阅器。` |\n| 509 | `self.count = 0  # 包计数。` |\n| 510 | `self.last_msg = 0.0  # 最近消息。` |\n| 511 | `self.last_emit = 0.0  # 最近刷新。` |\n| 513 | `def stop(self) -> None:  # 停止。` |\n| 514 | `self.running = False  # 请求停止。` |\n| 515 | `if self.subscriber:  # 有订阅器。` |\n| 516 | `for name in ["Close", "close", "Stop", "stop", "Destroy", "destroy"]:  # 兼容关闭方法。` |\n| 517 | `fn = getattr(self.subscriber, name, None)  # 获取方法。` |\n| 518 | `if callable(fn):  # 可调用。` |\n| 519 | `try: fn(); break  # 关闭。` |\n| 520 | `except Exception: pass  # 忽略。` |\n| 522 | `def run(self) -> None:  # 线程入口。` |\n| 523 | `if self.cfg.protocol == "mock": self.run_mock(); return  # mock。` |\n| 524 | `if self.cfg.protocol == "sdk2": self.run_sdk2(); return  # sdk2。` |\n| 525 | `self.error_signal.emit(f"只支持 mock/sdk2，当前 protocol={self.cfg.protocol}")  # 不支持。` |\n| 527 | `def run_mock(self) -> None:  # mock LiDAR。` |\n| 528 | `self.log_signal.emit("mock LiDAR 线程启动。")  # 日志。` |\n| 529 | `while self.running:  # 循环。` |\n| 530 | `self.count += 1  # 计数。` |\n| 531 | `rpy = [random.uniform(-0.02, 0.02), random.uniform(-0.02, 0.02), random.uniform(-0.1, 0.1)]  # 姿态。` |\n| 532 | `self.status_signal.emit({"update_time": now_text(), "packet_count": str(self.count), "topic": "mock/lidar", "idl_type": "mock", "error_st...` |\n| 533 | `self.msleep(200)  # 5Hz。` |\n| 534 | `self.log_signal.emit("mock LiDAR 线程已停止。")  # 日志。` |\n| 536 | `def run_sdk2(self) -> None:  # SDK2 LiDAR。` |\n| 537 | `try:  # 捕获异常。` |\n| 538 | `from unitree_sdk2py.core.channel import ChannelSubscriber  # 订阅器。` |\n| 539 | `from unitree_sdk2py.idl.unitree_go.msg.dds_ import LidarState_  # LiDAR IDL。` |\n| 540 | `ensure_dds_initialized(self.cfg.network_interface, self.log_signal.emit)  # 初始化 DDS。` |\n| 541 | `self.subscriber = ChannelSubscriber(self.cfg.lidar_state_topic, LidarState_)  # 创建订阅。` |\n| 542 | `self.subscriber.Init(self.on_message, 10)  # 初始化订阅。` |\n| 543 | `self.last_msg = time.monotonic()  # 时间。` |\n| 544 | `warned = False  # 无数据提示标志。` |\n| 545 | `while self.running:  # 保持线程。` |\n| 546 | `if time.monotonic() - self.last_msg > 3 and not warned:  # 3 秒无数据。` |\n| 547 | `warned = True  # 避免重复。` |\n| 548 | `self.log_signal.emit("超过 3 秒未收到 LiDAR State，请检查 Topic、网卡和 DDS。")  # 日志。` |\n| 549 | `self.msleep(100)  # 降 CPU。` |\n| 550 | `except Exception as exc:  # 错误。` |\n| 551 | `self.error_signal.emit(f"SDK2 LiDAR State 读取失败：{exc}")  # 报错。` |\n| 553 | `def on_message(self, msg: Any) -> None:  # 回调。` |\n| 554 | `if not self.running: return  # 停止后忽略。` |\n| 555 | `self.count += 1  # 计数。` |\n| 556 | `self.last_msg = time.monotonic()  # 更新时间。` |\n| 557 | `if self.last_msg - self.last_emit < 0.1: return  # 限速。` |\n| 558 | `self.last_emit = self.last_msg  # 更新时间。` |\n| 559 | `self.status_signal.emit(extract_lidar_state(msg, self.cfg, self.count))  # 发状态。` |\n| 562 | `def load_ascii_pcd(path: Path, max_points: int = 80000) -> Tuple[np.ndarray, Optional[np.ndarray], int, int]:` |\n| 563 | `"""读取 ASCII PCD 的 xyz 和 intensity。"""` |\n| 564 | `if not path.exists():  # 文件不存在。` |\n| 565 | `raise FileNotFoundError(f"PCD 文件不存在：{path}")` |\n| 566 | `fields, data_type, skiprows, total = [], "", 0, 0  # 头部信息。` |\n| 567 | `with path.open("r", encoding="utf-8", errors="ignore") as f:  # 打开文件。` |\n| 568 | `for i, line in enumerate(f):  # 读头部。` |\n| 569 | `text = line.strip()  # 清理空白。` |\n| 570 | `upper = text.upper()  # 转大写。` |\n| 571 | `if upper.startswith("FIELDS"): fields = text.split()[1:]  # 字段。` |\n| 572 | `elif upper.startswith("POINTS"): total = as_int(text.split()[1]) or 0  # 点数。` |\n| 573 | `elif upper.startswith("DATA"): data_type, skiprows = text.split()[1].lower(), i + 1; break  # 数据类型。` |\n| 574 | `if data_type != "ascii":  # 只支持 ASCII。` |\n| 575 | `raise RuntimeError(f"当前读取器只支持 ASCII PCD，当前 DATA={data_type}")` |\n| 576 | `for required in ["x", "y", "z"]:  # 必要字段。` |\n| 577 | `if required not in fields: raise RuntimeError(f"PCD 缺少字段：{required}")  # 缺字段。` |\n| 578 | `raw = np.loadtxt(str(path), skiprows=skiprows, dtype=np.float32)  # 读取数据。` |\n| 579 | `if raw.ndim == 1: raw = raw.reshape(1, -1)  # 单点转二维。` |\n| 580 | `original = raw.shape[0]  # 原始点数。` |\n| 581 | `if original > max_points: raw = raw[::max(1, math.ceil(original / max_points))]  # 抽样。` |\n| 582 | `xyz = raw[:, [fields.index("x"), fields.index("y"), fields.index("z")]]  # 坐标。` |\n| 583 | `intensity = raw[:, fields.index("intensity")] if "intensity" in fields else None  # 强度。` |\n| 584 | `return xyz, intensity, total or original, original  # 返回数据。` |\n| 587 | `class PointCloudCanvas(QWidget):` |\n| 588 | `"""PCD 点云显示控件。"""` |\n| 589 | `def __init__(self, parent=None) -> None:  # 初始化。` |\n| 590 | `super().__init__(parent)  # 父类初始化。` |\n| 591 | `self.ax = None  # 坐标轴。` |\n| 592 | `self.center = None  # 中心点。` |\n| 593 | `self.zoom = 1.0  # 缩放。` |\n| 594 | `self.base_range = (1.0, 1.0, 1.0)  # 基础范围。` |\n| 595 | `self.elev, self.azim = 60, -90  # 视角。` |\n| 596 | `self.dragging = False  # 拖拽状态。` |\n| 597 | `self.press = (0, 0, self.elev, self.azim)  # 按下状态。` |\n| 598 | `layout = QVBoxLayout()  # 布局。` |\n| 599 | `layout.setContentsMargins(0, 0, 0, 0)  # 无边距。` |\n| 600 | `self.figure = Figure(figsize=(6, 4))  # Figure。` |\n| 601 | `self.canvas = FigureCanvas(self.figure)  # 画布。` |\n| 602 | `layout.addWidget(self.canvas)  # 添加画布。` |\n| 603 | `self.setLayout(layout)  # 设置布局。` |\n| 604 | `self.canvas.mpl_connect("button_press_event", self.on_press)  # 鼠标按下。` |\n| 605 | `self.canvas.mpl_connect("motion_notify_event", self.on_move)  # 鼠标移动。` |\n| 606 | `self.canvas.mpl_connect("button_release_event", self.on_release)  # 鼠标释放。` |\n| 607 | `self.show_message("尚未加载点云地图")  # 初始提示。` |\n| 609 | `def show_message(self, text: str) -> None:  # 显示提示。` |\n| 610 | `self.figure.clear()  # 清图。` |\n| 611 | `ax = self.figure.add_subplot(111)  # 普通轴。` |\n| 612 | `ax.axis("off")  # 隐藏轴。` |\n| 613 | `ax.text(0.5, 0.5, text, ha="center", va="center", wrap=True)  # 文本。` |\n| 614 | `self.ax = ax  # 保存轴。` |\n| 615 | `self.canvas.draw_idle()  # 刷新。` |\n| 617 | `def plot_pcd(self, path: str) -> str:  # 绘制 PCD。` |\n| 618 | `xyz, intensity, total, original = load_ascii_pcd(Path(path))  # 读取 PCD。` |\n| 619 | `x, y, z = xyz[:, 0], xyz[:, 1], xyz[:, 2]  # 拆坐标。` |\n| 620 | `self.figure.clear()  # 清图。` |\n| 621 | `self.ax = self.figure.add_subplot(111, projection="3d")  # 3D 轴。` |\n| 622 | `color, label = (intensity, "intensity") if intensity is not None else (z, "Z")  # 上色字段。` |\n| 623 | `scatter = self.ax.scatter(x, y, z, c=color, s=1, cmap="viridis", depthshade=False)  # 绘点。` |\n| 624 | `cbar = self.figure.colorbar(scatter, ax=self.ax, shrink=0.68, pad=0.13, fraction=0.035, aspect=25)  # 色条。` |\n| 625 | `cbar.set_label(label, labelpad=10)  # 色条标签。` |\n| 626 | `self.ax.set_title("3D Point Cloud Map")  # 标题。` |\n| 627 | `self.ax.set_xlabel("X"); self.ax.set_ylabel("Y"); self.ax.set_zlabel("Z")  # 坐标标签。` |\n| 628 | `mins, maxs = xyz.min(axis=0), xyz.max(axis=0)  # 坐标范围。` |\n| 629 | `self.center = tuple((mins + maxs) / 2.0)  # 中心点。` |\n| 630 | `ranges = np.maximum(maxs - mins, 1e-6) * 1.10  # 基础范围。` |\n| 631 | `ranges[2] = max(ranges[2], max(ranges[0], ranges[1]) * 0.08)  # Z 最小范围。` |\n| 632 | `self.base_range = tuple(float(v) for v in ranges)  # 保存范围。` |\n| 633 | `self.zoom, self.elev, self.azim = 0.85, 60, -90  # 重置视角。` |\n| 634 | `self.apply_view()  # 应用视图。` |\n| 635 | `self.figure.subplots_adjust(left=0.02, right=0.78, bottom=0.02, top=0.92)  # 布局。` |\n| 636 | `self.canvas.draw_idle()  # 刷新。` |\n| 637 | `return f"已加载：{Path(path).name} \| 原始点数：{original} \| 显示点数：{len(xyz)} \| 图例：{label}"  # 日志。` |\n| 639 | `def apply_view(self) -> None:  # 应用视图。` |\n| 640 | `if self.ax is None or self.center is None: return  # 无图不处理。` |\n| 641 | `cx, cy, cz = self.center  # 中心。` |\n| 642 | `rx, ry, rz = [v * self.zoom for v in self.base_range]  # 当前范围。` |\n| 643 | `self.ax.set_xlim(cx - rx / 2, cx + rx / 2); self.ax.set_ylim(cy - ry / 2, cy + ry / 2); self.ax.set_zlim(cz - rz / 2, cz + rz / 2)  # 坐标范围。` |\n| 644 | `self.ax.xaxis.set_major_locator(MaxNLocator(nbins=5)); self.ax.yaxis.set_major_locator(MaxNLocator(nbins=5)); self.ax.zaxis.set_major_loc...` |\n| 645 | `if hasattr(self.ax, "set_box_aspect"): self.ax.set_box_aspect([rx, ry, rz])  # 坐标盒比例。` |\n| 646 | `self.ax.view_init(elev=self.elev, azim=self.azim)  # 视角。` |\n| 648 | `def zoom_in(self) -> None: self.zoom_action(0.8)  # 放大。` |\n| 649 | `def zoom_out(self) -> None: self.zoom_action(1.25)  # 缩小。` |\n| 651 | `def zoom_action(self, factor: float) -> None:  # 缩放动作。` |\n| 652 | `if self.center is None: return  # 无点云。` |\n| 653 | `self.zoom = max(0.03, min(30.0, self.zoom * factor))  # 限制缩放。` |\n| 654 | `self.apply_view(); self.canvas.draw_idle()  # 应用并刷新。` |\n| 656 | `def save_current_view(self) -> str:  # 保存视图。` |\n| 657 | `path, _ = QFileDialog.getSaveFileName(self, "保存当前点云视角", str(APP_DIR / "pointcloud_view.png"), "PNG Image (*.png);;JPEG Image (*.jpg);;PDF...` |\n| 658 | `if not path: return ""  # 取消。` |\n| 659 | `self.figure.savefig(path, dpi=200, bbox_inches="tight")  # 保存。` |\n| 660 | `return path  # 返回路径。` |\n| 662 | `def on_press(self, event) -> None:  # 鼠标按下。` |\n| 663 | `if event.button != 1: return  # 只处理左键。` |\n| 664 | `self.dragging = True; self.press = (event.x, event.y, self.elev, self.azim)  # 记录状态。` |\n| 666 | `def on_move(self, event) -> None:  # 鼠标移动。` |\n| 667 | `if not self.dragging or self.center is None: return  # 未拖拽。` |\n| 668 | `px, py, pe, pa = self.press  # 按下状态。` |\n| 669 | `self.azim = pa - (event.x - px) * 0.4  # 方位角。` |\n| 670 | `self.elev = max(-89, min(89, pe - (event.y - py) * 0.4))  # 仰角。` |\n| 671 | `self.apply_view(); self.canvas.draw_idle()  # 刷新。` |\n| 673 | `def on_release(self, event) -> None:  # 鼠标释放。` |\n| 674 | `self.dragging = False  # 停止拖拽。` |\n| 677 | `class H1RobotClient(QObject):` |\n| 678 | `"""连接协调器。"""` |\n| 679 | `log_signal = pyqtSignal(str)  # 日志。` |\n| 680 | `state_signal = pyqtSignal(bool)  # 连接状态。` |\n| 681 | `status_signal = pyqtSignal(dict)  # 状态数据。` |\n| 683 | `def __init__(self) -> None:  # 初始化。` |\n| 684 | `super().__init__()  # 父类。` |\n| 685 | `self.connected = False  # 是否连接。` |\n| 686 | `self.connecting = False  # 是否连接中。` |\n| 687 | `self.worker: Optional[LowStateWorker] = None  # LowState 线程。` |\n| 689 | `def connect_robot(self, cfg: RobotConfig) -> None:  # 连接。` |\n| 690 | `if self.connected or self.connecting: self.log_signal.emit("机器人已连接或正在连接。"); return  # 防重复。` |\n| 691 | `self.connecting = True; self.connected = False; self.state_signal.emit(False)  # 状态。` |\n| 692 | `self.worker = LowStateWorker(cfg)  # 创建线程。` |\n| 693 | `self.worker.ready_signal.connect(self.on_ready)  # 就绪。` |\n| 694 | `self.worker.fatal_signal.connect(self.on_fatal)  # 错误。` |\n| 695 | `self.worker.log_signal.connect(self.log_signal.emit)  # 日志。` |\n| 696 | `self.worker.status_signal.connect(self.status_signal.emit)  # 状态。` |\n| 697 | `self.worker.finished.connect(self.on_finished)  # 结束。` |\n| 698 | `self.worker.start()  # 启动。` |\n| 699 | `self.log_signal.emit("状态读取线程已启动。")  # 日志。` |\n| 701 | `def disconnect_robot(self) -> None:  # 断开。` |\n| 702 | `self.connecting = False  # 清连接中。` |\n| 703 | `if self.worker:  # 有线程。` |\n| 704 | `self.worker.stop(); self.worker.wait(3000); self.worker = None  # 停止线程。` |\n| 705 | `self.connected = False; self.state_signal.emit(False); self.log_signal.emit("已断开连接。")  # 状态。` |\n| 707 | `def send_command(self, command: str) -> None:  # 预留指令。` |\n| 708 | `if not self.connected: raise RuntimeError("机器人未连接，无法发送指令。")  # 未连接。` |\n| 709 | `self.log_signal.emit(f"指令接口为安全预留，不发布 LowCmd：{command}")  # 日志。` |\n| 711 | `def on_ready(self) -> None:  # 就绪。` |\n| 712 | `self.connecting = False; self.connected = True; self.state_signal.emit(True); self.log_signal.emit("状态读取初始化成功。")  # 状态。` |\n| 714 | `def on_fatal(self, text: str) -> None:  # 致命错误。` |\n| 715 | `self.log_signal.emit(text); self.connecting = False; self.connected = False; self.state_signal.emit(False)  # 状态。` |\n| 716 | `if self.worker: self.worker.stop()  # 停止。` |\n| 718 | `def on_finished(self) -> None:  # 线程结束。` |\n| 719 | `if not self.connected: self.worker = None  # 清引用。` |\n| 722 | `class LoginDialog(QDialog):` |\n| 723 | `"""登录窗口。"""` |\n| 724 | `def __init__(self) -> None:  # 初始化。` |\n| 725 | `super().__init__()  # 父类。` |\n| 726 | `self.user_profile = None  # 登录结果。` |\n| 727 | `self.store = UserStore()  # 用户仓库。` |\n| 728 | `self.setWindowTitle("H1 控制台登录")  # 标题。` |\n| 729 | `self.setMinimumSize(560, 520)  # 尺寸。` |\n| 730 | `layout = QVBoxLayout(self)  # 根布局。` |\n| 731 | `layout.setContentsMargins(44, 34, 44, 34)  # 边距。` |\n| 732 | `title = QLabel("H1 机器人控制面板")  # 标题。` |\n| 733 | `title.setAlignment(Qt.AlignCenter)  # 居中。` |\n| 734 | `title.setStyleSheet("font-size:26px;font-weight:900;color:white;")  # 样式。` |\n| 735 | `self.username_edit = QLineEdit("admin")  # 用户名。` |\n| 736 | `self.password_edit = QLineEdit("admin123")  # 密码。` |\n| 737 | `self.password_edit.setEchoMode(QLineEdit.Password)  # 密码模式。` |\n| 738 | `show_box = QCheckBox("显示密码")  # 显示密码。` |\n| 739 | `show_box.toggled.connect(lambda checked: self.password_edit.setEchoMode(QLineEdit.Normal if checked else QLineEdit.Password))  # 切换显示。` |\n| 740 | `login_btn = QPushButton("登录")  # 登录按钮。` |\n| 741 | `login_btn.clicked.connect(self.try_login)  # 登录。` |\n| 742 | `layout.addWidget(title); layout.addWidget(QLabel("用户名")); layout.addWidget(self.username_edit); layout.addWidget(QLabel("密码")); layout.ad...` |\n| 743 | `self.setStyleSheet("QDialog{background:#0f172a;color:white;font-family:'Microsoft YaHei';} QLineEdit{min-height:40px;border-radius:10px;p...` |\n| 745 | `def try_login(self) -> None:  # 登录。` |\n| 746 | `profile = self.store.authenticate(self.username_edit.text().strip(), self.password_edit.text())  # 校验。` |\n| 747 | `if not profile: QMessageBox.warning(self, "登录失败", "用户名或密码错误。"); return  # 失败。` |\n| 748 | `self.user_profile = profile; self.accept()  # 成功。` |\n| 751 | `class NavigationDialog(QDialog):` |\n| 752 | `"""SSH 导航窗口。"""` |\n| 753 | `log_signal = pyqtSignal(str)  # 日志。` |\n| 755 | `def __init__(self, robot_ip: str, parent=None) -> None:  # 初始化。` |\n| 756 | `super().__init__(parent)  # 父类。` |\n| 757 | `self.process: Optional[QProcess] = None  # 当前进程。` |\n| 758 | `self.setWindowTitle("H1 导航建图窗口")  # 标题。` |\n| 759 | `self.resize(900, 620)  # 尺寸。` |\n| 760 | `layout = QVBoxLayout(self)  # 根布局。` |\n| 761 | `form = QFormLayout()  # 表单。` |\n| 762 | `self.ip_edit = QLineEdit(robot_ip or "192.168.123.162")  # IP。` |\n| 763 | `self.user_edit = QLineEdit("unitree")  # 用户。` |\n| 764 | `self.password_edit = QLineEdit("Unitree0408")  # 密码。` |\n| 765 | `self.password_edit.setEchoMode(QLineEdit.Password)  # 密码模式。` |\n| 766 | `self.dir_edit = QLineEdit("ws/unitree_slam/build")  # 目录。` |\n| 767 | `self.iface_edit = QLineEdit("eth0")  # 网卡。` |\n| 768 | `for label, widget in [("机器人 IP：", self.ip_edit), ("SSH 用户：", self.user_edit), ("SSH 密码 / sudo 密码：", self.password_edit), ("远程目录：", self.d...` |\n| 769 | `btns = QHBoxLayout()  # 按钮行。` |\n| 770 | `start_btn, stop_btn, ifconfig_btn, clear_btn = QPushButton("启动导航建图"), QPushButton("停止导航程序"), QPushButton("查看远程 ifconfig"), QPushButton("清...` |\n| 771 | `start_btn.clicked.connect(self.start_navigation); stop_btn.clicked.connect(self.stop_navigation); ifconfig_btn.clicked.connect(self.run_i...` |\n| 772 | `for b in [start_btn, stop_btn, ifconfig_btn, clear_btn]: btns.addWidget(b)  # 添加按钮。` |\n| 773 | `self.output = QTextEdit(); self.output.setReadOnly(True)  # 输出框。` |\n| 774 | `layout.addLayout(form); layout.addLayout(btns); layout.addWidget(self.output)  # 添加布局。` |\n| 775 | `self.setStyleSheet("QDialog{background:#eef2f7;} QTextEdit{background:#020617;color:#dbeafe;font-family:Consolas;}")  # 样式。` |\n| 777 | `def append_output(self, text: str) -> None:  # 输出。` |\n| 778 | `self.output.append(f"[{now_text()}] {text}")  # 带时间戳。` |\n| 780 | `def ssh_prefix(self) -> str:  # SSH 前缀。` |\n| 781 | `return "sshpass -p " + shlex.quote(self.password_edit.text()) + " ssh -T -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null " + ...` |\n| 783 | `def run_command(self, command: str, title: str) -> None:  # 执行命令。` |\n| 784 | `if self.process and self.process.state() != QProcess.NotRunning: QMessageBox.information(self, "提示", "已有命令正在运行，请先停止。"); return  # 防重复。` |\n| 785 | `self.process = QProcess(self); self.process.setProcessChannelMode(QProcess.MergedChannels)  # 进程。` |\n| 786 | `self.process.readyReadStandardOutput.connect(self.on_output); self.process.readyReadStandardError.connect(self.on_output)  # 输出。` |\n| 787 | `self.process.finished.connect(lambda code, status: self.append_output(f"进程结束，exit_code={code}"))  # 结束。` |\n| 788 | `self.append_output(f"开始执行：{title}"); self.append_output(command); self.log_signal.emit(title)  # 日志。` |\n| 789 | `self.process.start("bash", ["-lc", command])  # 启动。` |\n| 791 | `def start_navigation(self) -> None:  # 启动导航。` |\n| 792 | `password, remote_dir, iface = self.password_edit.text(), self.dir_edit.text().strip(), self.iface_edit.text().strip()  # 参数。` |\n| 793 | `remote = f"set -e; cd {shlex.quote(remote_dir)}; export TERM=dumb NO_COLOR=1; export LD_LIBRARY_PATH=$PWD/../unitree_robotics/lib/$(uname...` |\n| 794 | `command = "command -v sshpass >/dev/null 2>&1 \|\| { echo '缺少 sshpass，请 sudo apt install sshpass'; exit 127; }; " + self.ssh_prefix() + "...` |\n| 795 | `self.run_command(command, "启动 demo_h1")  # 执行。` |\n| 797 | `def run_ifconfig(self) -> None:  # 查看 ifconfig。` |\n| 798 | `self.run_command(self.ssh_prefix() + " " + shlex.quote("ifconfig"), "查看远程 ifconfig")  # 执行。` |\n| 800 | `def stop_navigation(self) -> None:  # 停止。` |\n| 801 | `if self.process and self.process.state() != QProcess.NotRunning: self.process.terminate(); self.process.waitForFinished(1500); self.proce...` |\n| 802 | `password, iface = self.password_edit.text(), self.iface_edit.text().strip()  # 参数。` |\n| 803 | `remote = f"printf '%s\\n' {shlex.quote(password)} \| sudo -S pkill -f {shlex.quote('demo_h1 ' + iface)}"  # 远程停止。` |\n| 804 | `QProcess.startDetached("bash", ["-lc", self.ssh_prefix() + " " + shlex.quote(remote)])  # 后台停止。` |\n| 805 | `self.append_output("已发送停止 demo_h1 的命令。")  # 日志。` |\n| 807 | `def on_output(self) -> None:  # 输出回调。` |\n| 808 | `if not self.process: return  # 无进程。` |\n| 809 | `data = bytes(self.process.readAllStandardOutput()).decode("utf-8", errors="ignore") + bytes(self.process.readAllStandardError()).decode("...` |\n| 810 | `data = clean_terminal(data)  # 清洗。` |\n| 811 | `if data.strip(): self.output.insertPlainText(data + ("\n" if not data.endswith("\n") else ""))  # 显示。` |\n| 814 | `class MainWindow(QMainWindow):` |\n| 815 | `"""主窗口。"""` |\n| 816 | `def __init__(self, profile: Dict[str, str]) -> None:  # 初始化。` |\n| 817 | `super().__init__()  # 父类。` |\n| 818 | `self.profile = profile  # 用户。` |\n| 819 | `self.client = H1RobotClient()  # 客户端。` |\n| 820 | `self.lidar_worker: Optional[LidarStateWorker] = None  # LiDAR 线程。` |\n| 821 | `self.nav_window: Optional[NavigationDialog] = None  # 导航窗口。` |\n| 822 | `self.client.log_signal.connect(self.append_log); self.client.state_signal.connect(self.set_connected); self.client.status_signal.connect(...` |\n| 823 | `self.setWindowTitle("H1 机器人控制台")  # 标题。` |\n| 824 | `self.build_ui(); self.load_config(); self.resize(1100, 760)  # UI、配置、尺寸。` |\n| 825 | `self.append_log(f"用户 {profile['display_name']} 已登录，角色：{profile['role']}")  # 登录日志。` |\n| 827 | `def build_ui(self) -> None:  # 构建 UI。` |\n| 828 | `root = QWidget(); root_layout = QVBoxLayout(root)  # 中央控件。` |\n| 829 | `self.tabs = QTabWidget()  # 标签页。` |\n| 830 | `self.tabs.addTab(self.scroll(self.connection_tab()), "连接")  # 连接页。` |\n| 831 | `self.tabs.addTab(self.status_tab(), "实时状态")  # 状态页。` |\n| 832 | `self.tabs.addTab(self.scroll(self.navigation_tab()), "建图导航")  # 导航页。` |\n| 833 | `self.tabs.addTab(self.lidar_tab(), "激光雷达 / 点云地图")  # LiDAR 页。` |\n| 834 | `self.log_edit = QTextEdit(); self.log_edit.setReadOnly(True); self.log_edit.setMaximumHeight(150)  # 日志框。` |\n| 835 | `root_layout.addWidget(self.tabs); root_layout.addWidget(self.log_edit)  # 添加。` |\n| 836 | `self.setCentralWidget(root)  # 设置中央。` |\n| 837 | `self.status_bar = QStatusBar(); self.setStatusBar(self.status_bar)  # 状态栏。` |\n| 838 | `self.conn_label = QLabel("未连接"); self.status_bar.addPermanentWidget(self.conn_label)  # 连接状态。` |\n| 839 | `self.set_connected(False)  # 初始状态。` |\n| 840 | `self.setStyleSheet("QMainWindow{background:#eef2f7;} QGroupBox{font-weight:800;border:1px solid #cbd5e1;border-radius:10px;margin-top:10p...` |\n| 842 | `def scroll(self, widget: QWidget) -> QScrollArea:  # 包装滚动。` |\n| 843 | `area = QScrollArea(); area.setWidget(widget); area.setWidgetResizable(True); area.setFrameShape(QScrollArea.NoFrame); return area  # 返回滚动区。` |\n| 845 | `def connection_tab(self) -> QWidget:  # 连接页。` |\n| 846 | `page = QWidget(); layout = QGridLayout(page)  # 页面。` |\n| 847 | `group = QGroupBox("H1 连接配置"); form = QFormLayout(group)  # 配置组。` |\n| 848 | `self.ip_edit = QLineEdit("192.168.123.162"); self.port_spin = QSpinBox(); self.port_spin.setRange(1, 65535); self.port_spin.setValue(8080...` |\n| 849 | `self.iface_edit = QLineEdit("enx9c69d3565ef9"); self.protocol_combo = QComboBox(); self.protocol_combo.addItem("模拟模式", "mock"); self.prot...` |\n| 850 | `self.protocol_combo.setCurrentIndex(1); self.lowstate_topic_combo = QComboBox(); self.lowstate_topic_combo.addItems(["rt/lowstate", "rt/l...` |\n| 851 | `for label, widget in [("机器人 IP：", self.ip_edit), ("端口：", self.port_spin), ("网卡/接口名：", self.iface_edit), ("通信方式：", self.protocol_combo), (...` |\n| 852 | `btns = QHBoxLayout(); connect_btn, disconnect_btn, save_btn = QPushButton("连接并读取状态"), QPushButton("断开/停止读取"), QPushButton("保存配置")  # 按钮。` |\n| 853 | `connect_btn.clicked.connect(self.connect_robot); disconnect_btn.clicked.connect(self.disconnect_robot); save_btn.clicked.connect(self.sav...` |\n| 854 | `for b in [connect_btn, disconnect_btn, save_btn]: btns.addWidget(b)  # 添加按钮。` |\n| 855 | `form.addRow(btns)  # 添加按钮行。` |\n| 856 | `cmd = QGroupBox("常用指令接口预留"); cmd_layout = QGridLayout(cmd)  # 指令组。` |\n| 857 | `for text, command, r, c in [("急停", "emergency_stop", 0, 0), ("站立", "stand_up", 1, 0), ("坐下", "sit_down", 1, 1), ("使能电机", "enable_motors",...` |\n| 858 | `btn = QPushButton(text); btn.clicked.connect(lambda checked=False, x=command: self.send_command(x)); cmd_layout.addWidget(btn, r, c)  # 添...` |\n| 859 | `layout.addWidget(group, 0, 0); layout.addWidget(cmd, 0, 1); layout.setColumnStretch(0, 3); layout.setColumnStretch(1, 2)  # 布局。` |\n| 860 | `return page  # 返回。` |\n| 862 | `def status_tab(self) -> QWidget:  # 状态页。` |\n| 863 | `page = QWidget(); layout = QVBoxLayout(page); tabs = QTabWidget()  # 页面。` |\n| 864 | `self.lowstate_table = self.kv_table(); self.imu_table = self.kv_table(); self.bms_table = self.kv_table(); self.motor_table = QTableWidge...` |\n| 865 | `self.motor_table.verticalHeader().setVisible(False); self.motor_table.setEditTriggers(QTableWidget.NoEditTriggers)  # 电机表设置。` |\n| 866 | `for widget, title in [(self.lowstate_table, "低频状态主字段"), (self.imu_table, "惯性测量单元"), (self.bms_table, "电池管理系统"), (self.motor_table, "电机状态"...` |\n| 867 | `layout.addWidget(tabs); return page  # 返回。` |\n| 869 | `def navigation_tab(self) -> QWidget:  # 导航页。` |\n| 870 | `page = QWidget(); layout = QVBoxLayout(page); btn = QPushButton("打开导航建图窗口")  # 页面按钮。` |\n| 871 | `btn.clicked.connect(self.open_navigation); layout.addWidget(QLabel("点击按钮后会打开独立窗口，通过 SSH 远程启动 demo_h1。")); layout.addWidget(btn); layout.a...` |\n| 872 | `return page  # 返回。` |\n| 874 | `def lidar_tab(self) -> QWidget:  # LiDAR 页。` |\n| 875 | `page = QWidget(); layout = QHBoxLayout(page)  # 页面。` |\n| 876 | `left = QGroupBox("激光雷达状态"); left_layout = QVBoxLayout(left)  # 左侧。` |\n| 877 | `self.lidar_topic_edit = QLineEdit("rt/utlidar/lidar_state"); start_btn, stop_btn = QPushButton("开始读取 LiDAR"), QPushButton("停止读取")  # 控件。` |\n| 878 | `start_btn.clicked.connect(self.start_lidar); stop_btn.clicked.connect(self.stop_lidar)  # 绑定。` |\n| 879 | `self.lidar_table = self.kv_table()  # 表格。` |\n| 880 | `left_layout.addWidget(QLabel("状态 Topic：")); left_layout.addWidget(self.lidar_topic_edit); left_layout.addWidget(start_btn); left_layout.a...` |\n| 881 | `right = QGroupBox("PCD 点云地图"); right_layout = QVBoxLayout(right)  # 右侧。` |\n| 882 | `row = QHBoxLayout(); self.pcd_edit = QLineEdit(str(APP_DIR / "GlobalMap.pcd"))  # 路径。` |\n| 883 | `for text, slot in [("选择", self.browse_pcd), ("加载", self.load_pcd), ("+", lambda: self.pointcloud.zoom_in()), ("-", lambda: self.pointclou...` |\n| 884 | `btn = QPushButton(text); btn.clicked.connect(slot); row.addWidget(btn)  # 添加按钮。` |\n| 885 | `row.insertWidget(0, self.pcd_edit, stretch=1)  # 插入路径。` |\n| 886 | `self.pointcloud = PointCloudCanvas()  # 点云控件。` |\n| 887 | `right_layout.addLayout(row); right_layout.addWidget(self.pointcloud)  # 添加。` |\n| 888 | `layout.addWidget(left, 1); layout.addWidget(right, 2)  # 添加左右。` |\n| 889 | `QTimer.singleShot(300, self.load_pcd_if_exists)  # 自动加载。` |\n| 890 | `return page  # 返回。` |\n| 892 | `def kv_table(self) -> QTableWidget:  # 键值表。` |\n| 893 | `table = QTableWidget(); table.setColumnCount(2); table.setHorizontalHeaderLabels(["字段", "值"]); table.verticalHeader().setVisible(False); ...` |\n| 894 | `table.horizontalHeader().setSectionResizeMode(0, QHeaderView.ResizeToContents); table.horizontalHeader().setSectionResizeMode(1, QHeaderV...` |\n| 895 | `return table  # 返回。` |\n| 897 | `def fill_kv(self, table: QTableWidget, data: Dict[str, Any]) -> None:  # 填充键值表。` |\n| 898 | `table.setRowCount(len(data))  # 行数。` |\n| 899 | `for r, (k, v) in enumerate(data.items()):  # 遍历。` |\n| 900 | `table.setItem(r, 0, QTableWidgetItem(tr(k))); table.setItem(r, 1, QTableWidgetItem(str(v)))  # 设置单元格。` |\n| 902 | `def fill_motor(self, columns: List[str], rows: List[Dict[str, Any]]) -> None:  # 填充电机表。` |\n| 903 | `self.motor_table.setColumnCount(len(columns)); self.motor_table.setHorizontalHeaderLabels([tr(c) for c in columns]); self.motor_table.set...` |\n| 904 | `for r, row in enumerate(rows):  # 行。` |\n| 905 | `for c, name in enumerate(columns): self.motor_table.setItem(r, c, QTableWidgetItem(str(row.get(name, ""))))  # 单元格。` |\n| 906 | `self.motor_table.resizeColumnsToContents()  # 自适应。` |\n| 908 | `def get_config(self) -> RobotConfig:  # 从 UI 获取配置。` |\n| 909 | `return RobotConfig(robot_ip=self.ip_edit.text().strip(), port=int(self.port_spin.value()), network_interface=self.iface_edit.text().strip...` |\n| 911 | `def save_config(self) -> None:  # 保存配置。` |\n| 912 | `CONFIG_FILE.write_text(json.dumps(asdict(self.get_config()), ensure_ascii=False, indent=2), encoding="utf-8"); self.append_log("配置已保存。") ...` |\n| 914 | `def load_config(self) -> None:  # 加载配置。` |\n| 915 | `if not CONFIG_FILE.exists(): return  # 无文件。` |\n| 916 | `try:  # 捕获错误。` |\n| 917 | `cfg = RobotConfig(**{**asdict(RobotConfig()), **json.loads(CONFIG_FILE.read_text(encoding="utf-8"))})  # 合并默认值。` |\n| 918 | `self.ip_edit.setText(cfg.robot_ip); self.port_spin.setValue(cfg.port); self.iface_edit.setText(cfg.network_interface); self.lowstate_topi...` |\n| 919 | `for i in range(self.protocol_combo.count()):  # 找协议。` |\n| 920 | `if self.protocol_combo.itemData(i) == cfg.protocol: self.protocol_combo.setCurrentIndex(i)  # 设置。` |\n| 921 | `self.append_log("已加载本地配置。")  # 日志。` |\n| 922 | `except Exception as exc: self.append_log(f"加载配置失败：{exc}")  # 错误。` |\n| 924 | `def connect_robot(self) -> None: self.client.connect_robot(self.get_config())  # 连接。` |\n| 925 | `def disconnect_robot(self) -> None: self.client.disconnect_robot()  # 断开。` |\n| 927 | `def set_connected(self, connected: bool) -> None:  # 更新连接状态。` |\n| 928 | `self.conn_label.setText("已连接" if connected else "未连接"); self.conn_label.setStyleSheet("color:green;font-weight:800;" if connected else "c...` |\n| 930 | `def update_status(self, status: Dict[str, Any]) -> None:  # 刷新状态。` |\n| 931 | `self.fill_kv(self.lowstate_table, status.get("lowstate_main", {})); self.fill_kv(self.imu_table, status.get("imu_state", {})); self.fill_...` |\n| 933 | `def open_navigation(self) -> None:  # 打开导航窗口。` |\n| 934 | `if self.nav_window is None: self.nav_window = NavigationDialog(self.ip_edit.text().strip(), self); self.nav_window.log_signal.connect(sel...` |\n| 935 | `self.nav_window.show(); self.nav_window.raise_(); self.nav_window.activateWindow()  # 显示。` |\n| 937 | `def start_lidar(self) -> None:  # 启动 LiDAR。` |\n| 938 | `if self.lidar_worker and self.lidar_worker.isRunning(): QMessageBox.information(self, "提示", "LiDAR 已在运行。"); return  # 防重复。` |\n| 939 | `cfg = self.get_config(); self.lidar_worker = LidarStateWorker(cfg); self.lidar_worker.status_signal.connect(lambda data: self.fill_kv(sel...` |\n| 940 | `self.lidar_worker.start(); self.append_log(f"正在读取 LiDAR State：{cfg.lidar_state_topic}")  # 启动。` |\n| 942 | `def stop_lidar(self) -> None:  # 停止 LiDAR。` |\n| 943 | `if self.lidar_worker: self.lidar_worker.stop(); self.lidar_worker.wait(3000); self.lidar_worker = None  # 停止。` |\n| 944 | `self.append_log("已停止 LiDAR 状态读取。")  # 日志。` |\n| 946 | `def browse_pcd(self) -> None:  # 选择 PCD。` |\n| 947 | `path, _ = QFileDialog.getOpenFileName(self, "选择 PCD 点云文件", str(APP_DIR), "Point Cloud (*.pcd);;All Files (*)")  # 对话框。` |\n| 948 | `if path: self.pcd_edit.setText(path); self.load_pcd()  # 加载。` |\n| 950 | `def load_pcd_if_exists(self) -> None:  # 自动加载。` |\n| 951 | `if Path(self.pcd_edit.text().strip()).exists(): self.load_pcd()  # 存在就加载。` |\n| 953 | `def load_pcd(self) -> None:  # 加载点云。` |\n| 954 | `try: self.append_log(self.pointcloud.plot_pcd(self.pcd_edit.text().strip()))  # 绘制。` |\n| 955 | `except Exception as exc: self.pointcloud.show_message(str(exc)); self.append_log(f"加载 PCD 失败：{exc}")  # 错误。` |\n| 957 | `def save_pcd(self) -> None:  # 保存点云视图。` |\n| 958 | `path = self.pointcloud.save_current_view()  # 保存。` |\n| 959 | `if path: self.append_log(f"当前点云视角已保存：{path}")  # 日志。` |\n| 961 | `def send_command(self, command: str) -> None:  # 发送指令。` |\n| 962 | `if command == "emergency_stop" and QMessageBox.question(self, "确认急停", "确认发送急停指令？当前代码不会发布 LowCmd。", QMessageBox.Yes \| QMessageBox.No) != ...` |\n| 963 | `try: self.client.send_command(command)  # 发送。` |\n| 964 | `except Exception as exc: QMessageBox.warning(self, "指令失败", str(exc)); self.append_log(f"发送指令失败：{exc}")  # 错误。` |\n| 966 | `def append_log(self, text: str) -> None:  # 追加日志。` |\n| 967 | `self.log_edit.append(f"[{now_text()}] {text}")  # 时间戳。` |\n| 969 | `def closeEvent(self, event) -> None:  # 关闭窗口。` |\n| 970 | `if self.nav_window: self.nav_window.close()  # 关导航。` |\n| 971 | `self.stop_lidar(); self.client.disconnect_robot(); super().closeEvent(event)  # 停线程。` |\n| 974 | `def main() -> int:` |\n| 975 | `"""程序入口。"""` |\n| 976 | `QApplication.setAttribute(Qt.AA_EnableHighDpiScaling, True)  # 高 DPI。` |\n| 977 | `QApplication.setAttribute(Qt.AA_UseHighDpiPixmaps, True)  # 高清图片。` |\n| 978 | `setup_matplotlib_font()  # 字体。` |\n| 979 | `app = QApplication(sys.argv)  # 应用。` |\n| 980 | `app.setApplicationName("H1 机器人控制面板")  # 应用名。` |\n| 981 | `app.setFont(QFont("Noto Sans CJK SC", 10))  # 默认字体。` |\n| 982 | `login = LoginDialog()  # 登录框。` |\n| 983 | `if login.exec_() != QDialog.Accepted or not login.user_profile: return 0  # 取消。` |\n| 984 | `window = MainWindow(login.user_profile)  # 主窗口。` |\n| 985 | `window.show()  # 显示。` |\n| 986 | `return app.exec_()  # 事件循环。` |\n| 989 | `if __name__ == "__main__":` |\n| 990 | `sys.exit(main())  # 启动程序。` |